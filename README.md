# 한익스프레스 스마트오더 라벨 생성기

판매채널(**CU / GS / 이마트24**)의 스마트오더 RAW 파일을 업로드하면,
**코드 기반 매핑**으로 대월직매장 부착양식(A4) 라벨을 생성하는 웹앱입니다.

## 특징
- **화주사별 관리** — 상품·센터 마스터, 업로드 이력, 작업 집계가 **화주사 단위로 완전 분리**
  (`masters/<화주사id>/*.json`, `archive/<화주사id>/<채널>/`). 헤더의 🏢 선택으로 전환, 화주사 탭에서 추가/이름변경/삭제.
  기본 화주사는 오비맥주(`ob`), E24 라벨 제목의 화주사명도 자동 반영
- **시트 선택** — 업로드한 엑셀에 시트가 2개 이상이면 어느 시트로 라벨을 만들지 항상 확인
  (자동 인식된 시트가 기본 선택, 첫 행 미리보기 제공). 선택한 시트는 이력에 저장되어 다시 열 때도 유지
- **매핑**
  - **센터명 → 이고센터** — `masters/center.json` **전 채널 공통**. 센터코드를 안 쓰는 채널(E24)이 있어 이름 키로 통일.
    한 번 등록하면 CU·GS·이마트24에 모두 적용됨
  - **상품코드 → 한익스상품명 + 구분(대월/프리미엄)** — `masters/product.json` (전 채널 공통)
    - 같은 상품이라도 **채널마다 상품코드가 다르므로**, 한 한익스상품명에 CU/GS/E24 코드가 여러 개 매달림.
      마스터 관리 화면은 **한익스상품명 기준으로 묶어서** 보여주고, 기존 상품에 다른 채널 코드를 바로 추가할 수 있음
    - 이름을 잘못 타이핑해 같은 상품이 둘로 쪼개지지 않도록, 등록 시 기존 상품명을 목록에서 선택
  - 상품코드 미등록 시 화주상품명으로 폴백 조회(`product_names.json`) → 프리미엄 자동 분류
- **상품명 표기**: 부착양식 라벨=화주상품명(원본), TOTAL 표지=**항상 한익스상품명(대표)**
  - 한익스상품명이 마스터에 없는 상품이 있으면 **다운로드·인쇄가 차단**됨 → 화면에서 등록 후 재시도
- **직매장 구분**: 상품 구분에 따라 라벨 제목이 `대월직매장`/`프리미엄직매장` 으로 자동 분기
- **미매핑 코드 즉시 등록**: 업로드 중 안 잡힌 센터/상품 코드를 화면에서 바로 마스터에 추가 → 다음부터 자동 적용
- **마스터 관리** UI (조회·검색·추가·수정·삭제)
- **출력**
  - 부착양식(A4) **xlsx 다운로드** — 기존 양식과 동일한 스타일(맑은 고딕, 62/42/48pt, 테두리, 노란 번호)
    - 상품명·센터명이 길어 열 밖으로 잘릴 값은 **글자크기를 폭에 맞춰 자동 축소**(최소 22pt, 제목은 40pt)
      + 엑셀 '축소하여 전체 표시' 안전망 → 잘리는 글자 없음
  - **라벨출력(폼텍 디자인프로용) xlsx** — RAW 원본 컬럼 그대로 + `배송센터`(E24는 `한익스`)=이고센터 + `상품명(대표)`=한익스상품명
    을 덧붙인 `lowdata(라벨출력)` 시트. 1행=라벨 1장. 디자인프로에서 데이터 원본으로 불러 라벨 인쇄
  - 화면에서 **TOTAL 표지** 즉시 확인 + **클립보드 복사** + 표지 인쇄
  - 화면에서 **라벨 미리보기 + 바로 인쇄**(A4, 라벨 1장=1페이지)
- 라벨은 창고 분류에 맞춰 **구분 → 상품명(화주) → 이고센터 → 채널센터 → 점포명** 순 정렬
- **업로드 이력**: 업로드한 RAW 파일을 채널별로 영구 누적 보관 → 이력 화면에서 다시 열기·부착양식 다운로드·원본 다운로드·삭제
- **작업 집계(검수리스트)**: **채널·일자 선택** → **배송센터(이고센터) > 센터명 > 상품명** 그룹 집계(센터명 요약·총합계). CU는 발주단위수량 기준 → 화면·클립보드·엑셀

## 구성
```
app.py                 Flask 라우팅
smartorder_core.py     RAW 파싱 · 코드 매핑 · xlsx 생성 엔진
bootstrap_masters.py   기존 파일에서 seed 마스터 생성(1회용)
masters/*.json         코드 기반 마스터(seed)
templates/             화면(index/upload/result/print/masters...)
deploy/                Lightsail(nginx+systemd) 배포 스크립트
```

## 로컬 실행 (Windows)
```
py -m pip install -r requirements.txt
py app.py           # 또는 실행.bat 더블클릭 → 브라우저 자동 열림
```
기본 포트 5057 · http://127.0.0.1:5057/

## AWS Lightsail 배포 (Ubuntu)
```bash
curl -fsSL https://raw.githubusercontent.com/sangjin94/hanex_smartorder/main/deploy/setup-smartorder.sh | bash
# 코드 갱신
~/hanex_smartorder/deploy/update-smartorder.sh
```
- 앱은 gunicorn(127.0.0.1:8090) + systemd(`smartorder.service`), nginx 리버스 프록시
- **마스터는 `~/hanex_smartorder_data/masters`(git 밖)** 에 저장 → `git pull` 갱신에도 사용자가 수정한 매핑이 보존됨
- hanex_tool 사이트 하위경로(`/smartorder/`)로 붙이려면 `deploy/nginx-smartorder-location.conf` 참고 (앱이 `X-Forwarded-Prefix` 지원)

## RAW 인식 규칙
헤더 이름으로 컬럼을 자동 인식합니다.
- CU: `센터 코드`,`센터명`,`상품 코드`,`상품명`,`총수량`
- GS: `센터코드`,`센터`,`상품코드`,`상품명`,`수량` (xls/xlsx 모두)
- E24: `입고센터명`,`점포명`,`상품코드`,`앱 상품명`,`주문수량`
